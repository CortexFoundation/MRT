# MRT
Model Representation Template

## Setup

To set up the Python environment, run:

```bash
source python-env.sh
```

This will automatically add the `python` directory to your `PYTHONPATH`, allowing you to import modules from the `mrt` package.
